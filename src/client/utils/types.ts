export interface TChirps {
    id: Number,
    userid: Number,
    name: string,
    content: String,
    location: String,
    created_at: Date
}

export interface TUsers {
    id: Number,
    name: String,
    created_at: Date
}
